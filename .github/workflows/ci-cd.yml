name: CI/CD Pipeline for School Scheduler

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      # Checkout do código
      - name: Checkout repository
        uses: actions/checkout@v3

      # Configurar Python
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.13' # Use a versão compatível com seu ambiente

      # Instalar dependências
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install mysql-connector-python pulp

      # Executar testes
      - name: Run tests
        run: |
          python -m unittest tests/test_schedule.py
        env:
          DB_HOST: localhost
          DB_USER: root
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          DB_NAME: sistema_escolar

      # Construir e empacotar (opcional)
      - name: Build package
        run: |
          echo "Building application..."
          # Adicione comandos para empacotar (ex.: pyinstaller, se desejar um executável)

      # Implantação (simulada, ajuste conforme necessário)
      - name: Deploy
        run: |
          echo "Deploying to production..."
          # Adicione comandos para copiar o código para um servidor ou ambiente local