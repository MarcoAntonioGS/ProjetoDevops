apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
  namespace: school-scheduler-dev
type: Opaque
stringData:
  # EXAMPLE only: create secrets with kubectl or your secret manager in real environments
  user: root
  password: rootpassword
  rootpassword: rootpassword
